import { clearContainer } from './render.js';

export const clearSection = (container) => {
  clearContainer(container);
};

export const showEmptyWeather = () => {};

export const showEmptyForecast = () => {};
